"""
To run any of the tests, one must use the -m flag. For this to work, 
one should also write the file path using the python module notation.
i.e.: python -m test.aode
"""

import os
import sys
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))
